# Documented environment variables used by the frontend (do not commit real secrets)
# API base URL for backend (prefer this)
REACT_APP_API_BASE=https://your-backend.example.com

# Alternate backend URL if API base is not set
REACT_APP_BACKEND_URL=https://your-backend.example.com

# Optional: Feature flags in comma-separated key=value pairs.
# Voice is enabled by default if unspecified.
# Example to explicitly enable: REACT_APP_FEATURE_FLAGS="feature.voice=true"
# Example to disable: REACT_APP_FEATURE_FLAGS="feature.voice=false"
REACT_APP_FEATURE_FLAGS=feature.voice=true
